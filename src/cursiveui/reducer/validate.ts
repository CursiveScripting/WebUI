import { IUserProcess } from '../state/IUserProcess';
import { IProcess } from '../state/IProcess';


export function validate(process: IUserProcess, allProcesses: IProcess[]) {
    // TODO: oh yeah we need to do this
    return [];
}